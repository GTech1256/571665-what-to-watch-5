import PropTypes from "prop-types";

const PlayerScreenVideo = ({children}) => children;

PlayerScreenVideo.propTypes = {
  children: PropTypes.element.isRequired,
};

export default PlayerScreenVideo;
